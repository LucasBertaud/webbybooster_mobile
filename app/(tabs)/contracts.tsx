import {Text, View} from "react-native";

export default function ContractsPage() {
    return (
        <View style={{flex: 1, justifyContent: "center", alignItems: "center"}}>
            <Text>Missions & Contrats</Text>
        </View>
    );
}