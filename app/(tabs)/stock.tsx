import {Text, View} from "react-native";

export default function StockPage() {
    return (
        <View style={{flex: 1, justifyContent: "center", alignItems: "center"}}>
            <Text>Cours en stock</Text>
        </View>
    );
}